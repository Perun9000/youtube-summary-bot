# Local API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Клик по кнопке расширения в UI YouTube ставит ролик в очередь генерации через локальный HTTP-endpoint — пользователь остаётся на YouTube; при недоступности бота кнопка откатывается на старый t.me deep-link.

**Architecture:** Новый aiohttp-сервер `app/local_api.py` внутри контейнера (порт публикуется только на `127.0.0.1` хоста), маршрут `POST /enqueue` с токен-авторизацией → новая функция `enqueue_local_api_job(video_id, services)` в queue_service (постановка без aiogram Message, по образцу restored-jobs). В расширении fetch выполняет background service worker (content-script шлёт ему сообщение) — это обходит CORS/Private-Network-Access-ограничения Chrome; CORS/PNA-заголовки на сервере — страховка.

**Tech Stack:** Python 3.11, aiohttp (уже в зависимостях aiogram), pytest; browser-extension MV3 (Chrome/Arc).

**Spec:** docs/superpowers/specs/2026-07-10-local-api-design.md

## Global Constraints

- Порт наружу — ТОЛЬКО `127.0.0.1:8799:8799` в docker-compose.yml (loopback хоста).
- `LOCAL_API_TOKEN` пустой/отсутствует ИЛИ `OWNER_USER_ID` не задан → сервер НЕ стартует (фича выключена), бот работает как раньше.
- Deep-link-путь `/start` в боте НЕ меняется.
- Расширение без токена в настройках ведёт себя ровно как сейчас (только deep-link, никаких fetch).
- Сравнение токена — `hmac.compare_digest`.
- Тесты: сейчас 147/147, вывод pytest чистый. Async-тесты — по образцу существующих в репо (посмотри, как оформлены текущие async-тесты, и следуй той же конвенции).
- Коммиты английские, в конце `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Секреты (.env) не коммитить; docker не запускать (выкат делает оркестратор).

---

### Task 1: `enqueue_local_api_job` в queue_service

**Files:**
- Modify: `app/queue_service.py` (после `restore_pending_jobs`, ~строка 228)
- Test: `tests/test_local_api_enqueue.py` (новый)

**Interfaces:**
- Consumes: `_lookup_cached_summary(url, services, lang=...)` и `_deliver_cached_summary_for_job(job, services, cached)` из app/delivery.py; `SummaryJob`, `services.summary_queue_lock`, `services.job_store.add(...)`, `_summary_queue_worker` — всё уже в queue_service.
- Produces: `async def enqueue_local_api_job(video_id: str, services: Services) -> str` — возвращает `"cached"` | `"queued"`; кидает `RuntimeError("owner_not_configured")`, если `services.settings.owner_user_id is None`. Task 2 зовёт её из HTTP-хендлера.

- [ ] **Step 1: Failing tests**

`tests/test_local_api_enqueue.py` — по образцу существующих тестов очереди (см. tests/test_queue*.py: как строится фейковый Services). Ключевые кейсы:

```python
def test_enqueue_local_api_job_queues_for_owner():
    # services: owner_user_id=5779821, пустой кэш, реальные queue/lock, job_store-фейк
    result = asyncio.run(enqueue_local_api_job("dQw4w9WgXcQ", services))
    assert result == "queued"
    job = services.summary_queue.get_nowait()
    assert job.message is None
    assert job.chat_id == 5779821
    assert job.quota_user_id is None          # владелец — безлимит
    assert job.url == "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
    assert job.lang == "ru"                    # из user_langs-фейка (manual ru)

def test_enqueue_local_api_job_cache_hit_returns_cached():
    # кэш содержит саммари этого ролика; delivery-функция замокана
    result = asyncio.run(enqueue_local_api_job("dQw4w9WgXcQ", services))
    assert result == "cached"
    assert services.summary_queue.qsize() == 0  # в очередь не попало

def test_enqueue_local_api_job_without_owner_raises():
    # settings.owner_user_id = None
    with pytest.raises(RuntimeError, match="owner_not_configured"):
        asyncio.run(enqueue_local_api_job("dQw4w9WgXcQ", services))
```

Язык: фейковый `services.user_langs.get(owner_id)` возвращает `("ru", "manual")`; отдельный кейс — user_langs пуст → lang="ru" (owner-контекст, дефолт как у scheduled).

- [ ] **Step 2: Verify RED** — `./.venv/bin/pytest tests/test_local_api_enqueue.py -v` → ImportError/AttributeError.

- [ ] **Step 3: Implementation** (в `app/queue_service.py`):

```python
async def enqueue_local_api_job(video_id: str, services: Services) -> str:
    """Постановка от локального API (кнопка расширения, HTTP без Message).

    Аналог восстановленных задач: message=None, доставка и статусы идут через
    bot.send_message(chat_id владельца). Квоты не применяются (owner).
    Возвращает "cached" (отдано из кэша) или "queued".
    """
    owner_id = services.settings.owner_user_id
    if owner_id is None:
        raise RuntimeError("owner_not_configured")
    lang = "ru"
    if services.user_langs is not None:
        stored = services.user_langs.get(owner_id)
        if stored is not None:
            lang = stored[0]
    url = f"https://www.youtube.com/watch?v={video_id}"

    cached = _lookup_cached_summary(url, services, lang=lang)
    if cached is not None:
        probe = SummaryJob(
            sequence=0, message=None, url=url,
            enqueued_at=time.monotonic(), chat_id=owner_id, lang=lang,
        )
        await _deliver_cached_summary_for_job(probe, services, cached)
        logger.info("local_api.cache.hit video_id=%s", video_id)
        return "cached"

    async with services.summary_queue_lock:
        services.summary_next_sequence += 1
        db_id = (
            services.job_store.add(
                url, owner_id, scheduled=False, disable_notification=False,
                title_hint=None, lang=lang,
            )
            if services.job_store
            else None
        )
        job = SummaryJob(
            sequence=services.summary_next_sequence,
            message=None,
            url=url,
            enqueued_at=time.monotonic(),
            chat_id=owner_id,
            db_id=db_id,
            lang=lang,
        )
        await services.summary_queue.put(job)
        if services.summary_worker_task is None or services.summary_worker_task.done():
            services.summary_worker_task = asyncio.create_task(_summary_queue_worker(services))
    logger.info("local_api.job.enqueued sequence=%s video_id=%s", job.sequence, video_id)
    return "queued"
```

Импорты `_lookup_cached_summary`, `_deliver_cached_summary_for_job` — так же (и там же), как их уже импортирует queue_service; если сейчас импортирован только `_lookup_cached_summary` — добавь второй тем же стилем.

- [ ] **Step 4: Verify GREEN** — `./.venv/bin/pytest tests/test_local_api_enqueue.py -v` → PASS; полный suite `./.venv/bin/pytest tests/ -q` → 147 + новые.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "Add local-API enqueue path (no aiogram Message, owner-targeted)" ...`

---

### Task 2: aiohttp-сервер `app/local_api.py` + конфиг + wiring + docker-compose

**Files:**
- Create: `app/local_api.py`
- Modify: `app/config.py` (поля `local_api_token: str`, `local_api_port: int`), `app/main.py` (старт/стоп рядом с polling), `docker-compose.yml` (ports), `.env.example`
- Test: `tests/test_local_api_http.py` (новый)

**Interfaces:**
- Consumes: `enqueue_local_api_job(video_id, services) -> str` из Task 1; `YOUTUBE_VIDEO_ID_RE` из app/services_container.py; `Settings.local_api_token/local_api_port/owner_user_id`.
- Produces: `create_local_api_app(services) -> aiohttp.web.Application`; `async def start_local_api(services) -> web.AppRunner | None` (None = выключено); `async def stop_local_api(runner: web.AppRunner | None) -> None`.

- [ ] **Step 1: Failing tests**

`tests/test_local_api_http.py` (aiohttp.test_utils; создание клиента — `TestServer(create_local_api_app(services))` + `TestClient`, внутри asyncio.run или конвенции репо):

```python
def test_enqueue_requires_token():        # без X-Auth-Token → 401
def test_enqueue_rejects_wrong_token():   # неверный токен → 401 (hmac-путь)
def test_enqueue_rejects_bad_video_id():  # {"video_id": "short"} → 400
def test_enqueue_queued():                # валидно → 200 {"status":"queued"} (enqueue_local_api_job замокан)
def test_enqueue_cached():                # мок вернул "cached" → 200 {"status":"cached"}
def test_enqueue_internal_error():        # мок кинул Exception → 500 {"error":"internal"}
def test_options_preflight_cors():        # OPTIONS → 204, Access-Control-Allow-Origin: https://www.youtube.com,
                                          # Access-Control-Allow-Headers содержит X-Auth-Token,
                                          # Access-Control-Allow-Private-Network: true
def test_start_disabled_without_token():  # token="" → start_local_api вернул None
def test_start_disabled_without_owner():  # owner_user_id=None → None + warning-лог
```

- [ ] **Step 2: Verify RED.**

- [ ] **Step 3: Implementation.**

`app/config.py`: по образцу существующих полей — `local_api_token = os.getenv("LOCAL_API_TOKEN", "").strip()`, `local_api_port` через существующий int-парсер с дефолтом 8799; прокинуть в `Settings(...)`.

`app/local_api.py`:

```python
"""Локальный HTTP API для «тихой» постановки роликов из browser-extension.

Порт публикуется docker-compose'ом только на 127.0.0.1 хоста; авторизация —
статический токен LOCAL_API_TOKEN (hmac.compare_digest). CORS/PNA-заголовки
отдаются для https://www.youtube.com — страховка на случай fetch из
content-script (основной путь расширения — background service worker,
которому CORS не нужен).
"""
from __future__ import annotations

import hmac
import logging

from aiohttp import web

from app.queue_service import enqueue_local_api_job
from app.services_container import YOUTUBE_VIDEO_ID_RE

logger = logging.getLogger(__name__)

ALLOWED_ORIGIN = "https://www.youtube.com"


def _cors_headers() -> dict[str, str]:
    return {
        "Access-Control-Allow-Origin": ALLOWED_ORIGIN,
        "Access-Control-Allow-Methods": "POST, OPTIONS",
        "Access-Control-Allow-Headers": "Content-Type, X-Auth-Token",
        "Access-Control-Allow-Private-Network": "true",
    }


def create_local_api_app(services) -> web.Application:
    token = services.settings.local_api_token

    async def enqueue(request: web.Request) -> web.Response:
        supplied = request.headers.get("X-Auth-Token", "")
        if not (supplied and hmac.compare_digest(supplied, token)):
            return web.json_response({"error": "unauthorized"}, status=401, headers=_cors_headers())
        try:
            payload = await request.json()
        except Exception:  # noqa: BLE001
            return web.json_response({"error": "bad_json"}, status=400, headers=_cors_headers())
        video_id = str(payload.get("video_id", ""))
        if not YOUTUBE_VIDEO_ID_RE.fullmatch(video_id):
            return web.json_response({"error": "bad_video_id"}, status=400, headers=_cors_headers())
        try:
            status = await enqueue_local_api_job(video_id, services)
        except Exception:  # noqa: BLE001
            logger.exception("local_api.enqueue_failed video_id=%s", video_id)
            return web.json_response({"error": "internal"}, status=500, headers=_cors_headers())
        return web.json_response({"status": status}, headers=_cors_headers())

    async def preflight(request: web.Request) -> web.Response:
        return web.Response(status=204, headers=_cors_headers())

    app = web.Application()
    app.router.add_post("/enqueue", enqueue)
    app.router.add_options("/enqueue", preflight)
    return app


async def start_local_api(services) -> web.AppRunner | None:
    s = services.settings
    if not s.local_api_token:
        logger.info("local_api.boot enabled=false reason=no_token")
        return None
    if s.owner_user_id is None:
        logger.warning("local_api.boot enabled=false reason=no_owner_user_id")
        return None
    runner = web.AppRunner(create_local_api_app(services))
    await runner.setup()
    site = web.TCPSite(runner, "0.0.0.0", s.local_api_port)
    await site.start()
    logger.info("local_api.boot enabled=true port=%s", s.local_api_port)
    return runner


async def stop_local_api(runner: web.AppRunner | None) -> None:
    if runner is not None:
        await runner.cleanup()
```

`app/main.py`: после старта deferred-scheduler — `local_api_runner = await start_local_api(services)`; в `finally` — `await stop_local_api(local_api_runner)` (первым, до отмены тасок).

`docker-compose.yml`: сервису bot добавить

```yaml
    ports:
      - "127.0.0.1:8799:8799"
```

`.env.example`: `LOCAL_API_TOKEN=` (пусто = выключено, комментарий) и `LOCAL_API_PORT=8799`.

- [ ] **Step 4: Verify GREEN** — таргет-тесты + полный suite + `./.venv/bin/python -m compileall -q app`.

- [ ] **Step 5: Commit.**

---

### Task 3: Расширение — background fetch, токен в options, ✅/fallback

**Files:**
- Create: `browser-extension/background.js`
- Modify: `browser-extension/manifest.json` (background SW, host_permissions, version 0.2.5), `browser-extension/content.js` (обработчики клика двух кнопок + SCRIPT_VERSION), `browser-extension/options.html`, `browser-extension/options.js` (поле токена), `browser-extension/README.md`

**Interfaces:**
- Consumes: HTTP-контракт Task 2 (`POST http://127.0.0.1:8799/enqueue`, заголовок `X-Auth-Token`, тело `{"video_id"}`; 200 → `{"status":"queued"|"cached"}`).
- Produces: сообщение `{type: "yt-summary-enqueue", videoId}` из content.js → background.js; ответ `{ok: true, status}` | `{ok: false}`.

- [ ] **Step 1: manifest.json** — добавить:

```json
  "background": { "service_worker": "background.js" },
  "host_permissions": ["http://127.0.0.1:8799/*"],
  "version": "0.2.5"
```

(Существующие ключи manifest не трогать; если host_permissions уже есть — дополнить массив.)

- [ ] **Step 2: background.js** (новый):

```javascript
// Фоновый воркер: выполняет fetch к локальному API бота.
// Из background-контекста с host_permissions запрос не подчиняется
// CORS/Private-Network-Access-ограничениям страницы youtube.com.
const API_URL = 'http://127.0.0.1:8799/enqueue';
const api = typeof browser !== 'undefined' ? browser : chrome;

function getToken() {
  return new Promise((resolve) => {
    try {
      const maybe = api.storage.sync.get({ localApiToken: '' });
      if (maybe && typeof maybe.then === 'function') {
        maybe.then((items) => resolve(items.localApiToken || ''));
        return;
      }
      api.storage.sync.get({ localApiToken: '' }, (items) => resolve((items && items.localApiToken) || ''));
    } catch (_) { resolve(''); }
  });
}

api.runtime.onMessage.addListener((msg, _sender, sendResponse) => {
  if (!msg || msg.type !== 'yt-summary-enqueue') return false;
  (async () => {
    const token = await getToken();
    if (!token) { sendResponse({ ok: false }); return; }
    try {
      const controller = new AbortController();
      const timer = setTimeout(() => controller.abort(), 1500);
      const resp = await fetch(API_URL, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-Auth-Token': token },
        body: JSON.stringify({ video_id: msg.videoId }),
        signal: controller.signal,
      });
      clearTimeout(timer);
      if (resp.ok) {
        const data = await resp.json().catch(() => ({}));
        sendResponse({ ok: true, status: data.status || 'queued' });
        return;
      }
    } catch (_) { /* бот выключен / таймаут */ }
    sendResponse({ ok: false });
  })();
  return true; // асинхронный sendResponse
});
```

- [ ] **Step 3: content.js** — общий helper + правка ОБОИХ кликов (главная кнопка `buildButton`, ~строка 86, и превью-кнопки, ~строка 234):

```javascript
  // Пытается поставить ролик через локальный API (background SW).
  // true — успех (остаёмся на YouTube), false — нужен deep-link fallback.
  function tryLocalEnqueue(videoId) {
    return new Promise((resolve) => {
      try {
        api.runtime.sendMessage({ type: 'yt-summary-enqueue', videoId }, (resp) => {
          if (api.runtime.lastError || !resp) { resolve(false); return; }
          resolve(!!resp.ok);
        });
      } catch (_) { resolve(false); }
    });
  }

  function flashButton(btn, originalText) {
    btn.textContent = '✅';
    setTimeout(() => { btn.textContent = originalText; }, 2000);
  }
```

В обработчиках кликов вместо немедленного `window.open(url, ...)`:

```javascript
      const okLocal = await tryLocalEnqueue(id);
      if (okLocal) { flashButton(btn, '🔮 Summary'); return; }
      const handle = await getBotHandle();
      const url = `https://t.me/${handle}?start=${encodeURIComponent(id)}`;
      window.open(url, '_blank', 'noopener,noreferrer');
```

(Для превью-кнопки исходный текст — её текущий эмодзи; сохранить в переменную до замены.) `api` — тот же `browser||chrome`-алиас, что уже используется в content.js для storage; если алиаса нет — добавить в начало файла тем же паттерном, что в options.js. Обнови SCRIPT_VERSION-маркер до '0.2.5'.

- [ ] **Step 4: options.html/options.js** — поле «Local API token» (input type=password, key `localApiToken`, дефолт ''), сохранение/загрузка через существующие dual-form helpers `storageSyncGet/Set` рядом с botHandle. Подпись под полем: «Токен из LOCAL_API_TOKEN в .env бота. Пусто — кнопка работает через Telegram deep-link, как раньше.»

- [ ] **Step 5: README.md расширения** — раздел «Тихая генерация (Local API)»: как сгенерировать токен (`openssl rand -hex 24`), прописать в .env бота (LOCAL_API_TOKEN=...) и в настройках расширения; поведение ✅/fallback; перезагрузка расширения после обновления.

- [ ] **Step 6: Manual checks note** — автотестов для расширения нет; в отчёте перечислить ручной чеклист: (1) токен задан, бот работает → ✅ и саммари в TG, фокус на YouTube; (2) контейнер остановлен → deep-link fallback ≤2 сек; (3) токен пуст → deep-link сразу; (4) превью-кнопка ведёт себя так же.

- [ ] **Step 7: Commit.**

---

## Верификация после всех задач (оркестратор)

- Полный suite + compileall.
- Выкат: проверить очередь (`sqlite3 data/bot.db ...`), сгенерировать токен, дописать `LOCAL_API_TOKEN` в .env (точечная правка), `docker compose up -d --build`, лог `local_api.boot enabled=true port=8799`.
- Смоук с хоста: `curl -s -X POST http://127.0.0.1:8799/enqueue -H 'X-Auth-Token: <token>' -H 'Content-Type: application/json' -d '{"video_id":"<кэшированный id>"}'` → `{"status":"cached"}`.
- Владелец: вписать токен в options расширения, перезагрузить расширение, проверить кнопку.
